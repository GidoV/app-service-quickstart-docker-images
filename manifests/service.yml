apiVersion: v1
kind: Service
metadata:
    name: gidovappservicequickstartdockerimages
spec:
    type: LoadBalancer
    ports:
    - port: 2222 
    selector:
        app: gidovappservicequickstartdockerimages